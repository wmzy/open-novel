# 单阶段深度迭代（Deepen）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户从视图页面点 🔁 深化按钮，设定截止时间后，AI 在当前阶段自主循环深化——每轮按结构化维度评分、验证→回溯→修订，直到到点/饱和/手动中断。

**Architecture:** 纯前端驱动。`src/shared/deepen.ts` 提供维度定义+消息构造+饱和检测；各视图加按钮 dispatch 事件；ChatPanel 监听后进入 deepenMode 状态机，run 完成后自动续轮。后端零改动（复用 night-explore 的 autonomous 基础设施）。

**Tech Stack:** React 19, TypeScript, Linaria (CSS-in-JS), Vitest.

**Spec:** `docs/superpowers/specs/2026-07-08-deepen-design.md`

---

## File Structure

| 文件 | 动作 | 职责 |
|------|------|------|
| `src/shared/deepen.ts` | 新建 | 维度定义、事件常量、消息构造、饱和检测 |
| `src/web/components/ChatPanel.tsx` | 改 | 监听 DEEPEN 事件、deepenMode 状态机、截止时间弹框、续轮逻辑、状态指示条 |
| `src/web/components/views/CharacterView.tsx` | 改 | 加 🔁 深化按钮 |
| `src/web/components/views/WorldView.tsx` | 改 | 加 🔁 深化按钮 |
| `src/web/components/views/ConceptView.tsx` | 改 | 加 🔁 深化按钮 |
| `src/web/components/views/OutlineView.tsx` | 改 | 加 🔁 深化按钮 |
| `src/web/components/views/SceneView.tsx` | 改 | 加 🔁 深化按钮 |
| `tests/unit/shared/deepen.test.ts` | 新建 | 维度/消息/饱和检测单测 |

---

## Task 1: 核心共享模块 `src/shared/deepen.ts`

**Files:**
- Create: `src/shared/deepen.ts`
- Test: `tests/unit/shared/deepen.test.ts`

- [ ] **Step 1: 写失败的测试**

创建 `tests/unit/shared/deepen.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import {
  DEEPEN_TO_CHAT_EVENT,
  DEEPEN_DIMENSIONS,
  SATURATION_SIGNAL,
  buildDeepenMessage,
  detectSaturation,
  parseDeadlineInput,
} from '../../../src/shared/deepen';

describe('deepen', () => {
  describe('DEEPEN_DIMENSIONS', () => {
    it('has dimensions for all 5 planning stages', () => {
      const stages = ['concept', 'world', 'characters', 'outline', 'scenes'];
      for (const stage of stages) {
        expect(DEEPEN_DIMENSIONS[stage]).toBeDefined();
        expect(DEEPEN_DIMENSIONS[stage].length).toBeGreaterThanOrEqual(4);
      }
    });

    it('each dimension is a non-empty string with descriptive text', () => {
      for (const stage of Object.keys(DEEPEN_DIMENSIONS)) {
        for (const dim of DEEPEN_DIMENSIONS[stage]) {
          expect(dim.length).toBeGreaterThan(5);
          expect(dim).toContain('：'); // 包含说明性冒号
        }
      }
    });
  });

  describe('buildDeepenMessage', () => {
    it('includes stage name and round number', () => {
      const msg = buildDeepenMessage('characters', 3);
      expect(msg).toContain('角色');
      expect(msg).toContain('第 3 轮');
    });

    it('includes the dimensions for the stage', () => {
      const msg = buildDeepenMessage('characters', 1);
      // characters 维度中有动机清晰度
      expect(msg).toContain('动机清晰度');
      expect(msg).toContain('弧光完整性');
    });

    it('includes verification-backtracking flow guidance', () => {
      const msg = buildDeepenMessage('world', 1);
      expect(msg).toContain('验证');
      expect(msg).toContain('回溯');
      expect(msg).toContain('修订');
    });

    it('includes saturation signal instructions', () => {
      const msg = buildDeepenMessage('characters', 1);
      expect(msg).toContain('4 分以上');
      expect(msg).toContain(SATURATION_SIGNAL);
    });

    it('forbids question tool and stage advancement', () => {
      const msg = buildDeepenMessage('outline', 1);
      expect(msg).toContain('不要用 question 工具');
      expect(msg).toContain('不要推进到下一阶段');
    });
  });

  describe('detectSaturation', () => {
    it('detects saturation signal in log content', () => {
      const log = `## 第3轮\n**维度评分**：动机 5, 关系 4\n${SATURATION_SIGNAL}`;
      expect(detectSaturation(log)).toBe(true);
    });

    it('returns false when no saturation signal', () => {
      const log = '## 第2轮\n- 发现：角色动机不足\n- 改进：补充背景';
      expect(detectSaturation(log)).toBe(false);
    });
  });

  describe('parseDeadlineInput', () => {
    it('parses HH:MM to today timestamp', () => {
      const ts = parseDeadlineInput('06:00');
      const date = new Date(ts);
      expect(date.getHours()).toBe(6);
      expect(date.getMinutes()).toBe(0);
    });

    it('handles empty input by returning null', () => {
      expect(parseDeadlineInput('')).toBeNull();
    });

    it('if time already passed today, sets to tomorrow', () => {
      // 23:59 在当前时间之后（除非刚好午夜前），测试用固定值
      const ts = parseDeadlineInput('23:59');
      expect(ts).not.toBeNull();
      expect(ts).toBeGreaterThan(Date.now() - 86400000);
    });
  });

  describe('DEEPEN_TO_CHAT_EVENT', () => {
    it('is a non-empty string constant', () => {
      expect(typeof DEEPEN_TO_CHAT_EVENT).toBe('string');
      expect(DEEPEN_TO_CHAT_EVENT.length).toBeGreaterThan(0);
    });
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `npx vitest run tests/unit/shared/deepen.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `src/shared/deepen.ts`**

```typescript
/**
 * 单阶段深度迭代（Deepen）共享模块
 *
 * 提供：各阶段结构化质量维度、深化消息构造、饱和检测、事件常量。
 * 被 ChatPanel（监听事件 + 驱动循环）和各视图（dispatch 事件）共用。
 */

/** 视图 → ChatPanel 的事件名（dispatch CustomEvent） */
export const DEEPEN_TO_CHAT_EVENT = 'open-novel:deepen-to-chat';

/** 饱和信号标记——agent 写入 deepen-log 表示各维度已达标，前端检测后停止循环 */
export const SATURATION_SIGNAL = '[饱和信号：各维度已达 4+ 分，无明显可改进项]';

/** 事件 detail 类型 */
export interface DeepenToChatDetail {
  /** 深化的阶段：concept / world / characters / outline / scenes */
  stage: string;
}

/**
 * 各阶段的结构化质量维度（1-5 分自评框架）。
 * 来源：Self-Refine（结构化评分 > 泛泛反思）+ R2-Write（写作需触发验证/回溯）。
 */
export const DEEPEN_DIMENSIONS: Record<string, string[]> = {
  characters: [
    '动机清晰度：每个主要角色的驱动力三角（外在目标/内在需求/核心缺陷）是否具体、独特？',
    '关系丰富度：角色间关系是否有层次（对立/同盟/暧昧/转变）？',
    '弧光完整性：主角是否有清晰的变化轨迹（起点→转折→终点）？',
    '差异化程度：角色声音/行事风格是否可区分，避免千人一面？',
    '功能性覆盖：是否缺少叙事必需的功能性角色（导师/镜像/催化剂）？',
  ],
  world: [
    '体系自洽性：力量/社会/经济体系内部是否有矛盾？',
    '历史纵深：世界是否有可信的历史背景和因果链？',
    '文化丰富度：不同地域/阶层是否有差异化的文化特征？',
    '冲突潜力：世界设定是否孕育了多种潜在冲突源？',
    '感官沉浸：环境描写是否有视听嗅味触的多感官细节？',
  ],
  outline: [
    '三幕结构：起承转合是否清晰、节奏是否合理？',
    '因果链紧密度：事件之间是否有因果驱动而非巧合？',
    '伏笔密度：埋设与回收是否成对且分布合理？',
    '情感节奏：高低潮交替是否张弛有度？',
    '主题贯穿：核心主题是否在各幕中得到递进体现？',
  ],
  scenes: [
    '场景目的性：每个场景是否推进了情节或揭示了角色？',
    '主动被动交替：Scene/Sequel 是否合理交替？',
    '冲突烈度：场景内冲突是否有升级和转折？',
    '感官落地：场景是否有具体的感官细节而非纯对话？',
    '信息节制：是否避免了信息倾泻（info-dump）？',
  ],
  concept: [
    '核心冲突锐度：故事的核心矛盾是否清晰、有力？',
    '主题深度：道德前提是否有探讨价值，非说教？',
    '独特性：概念是否有区别于同类作品的差异化点？',
    '情感钩子：开头是否能抓住读者情感？',
    '可展开性：概念是否支撑长篇叙事的体量？',
  ],
};

/** 阶段中文名（用于消息展示） */
const STAGE_LABELS: Record<string, string> = {
  characters: '角色',
  world: '世界观',
  outline: '大纲',
  scenes: '场景',
  concept: '概念',
};

/**
 * 构造深化 message。融合三个调研优化：
 * - 结构化维度评分（优化 1）
 * - 验证→回溯→修订思维引导（优化 2）
 * - 饱和信号指令（优化 3）
 */
export function buildDeepenMessage(stage: string, round: number): string {
  const label = STAGE_LABELS[stage] || stage;
  const dimensions = DEEPEN_DIMENSIONS[stage] || [];

  return `你在做「${label}」阶段的深化打磨，这是第 ${round} 轮迭代。

## 评估维度
${dimensions.map((d, i) => `${i + 1}. ${d}`).join('\n')}

## 流程（验证→回溯→修订）
1. 读取当前阶段的产出文件
2. 读取 .novel/deepen-log.md 了解前几轮的评分和改进历史
3. **验证**：逐维度检查当前产出是否满足质量标准，给每个维度打 1-5 分
4. **回溯**：对最低分维度，分析根因（是缺少信息？逻辑断裂？还是深度不够？）
5. **修订**：针对根因做具体补充，而非表面润色
6. 修改完后在 .novel/deepen-log.md 追加本轮记录，格式：
   ## 第${round}轮
   **维度评分**：<维度名 旧分→新分, ...>
   - 发现：<本轮识别的最低分维度及其根因>
   - 改进：<做了什么具体修改>
   - 下轮建议：<下一轮值得关注的方向>
7. 不要用 question 工具提问，不要推进到下一阶段

## 饱和信号
如果本轮评估后发现所有维度已达 4 分以上，且没有明显可改进项，
在 deepen-log 本轮记录末尾另起一行写：${SATURATION_SIGNAL}
系统将自动停止循环。`;
}

/**
 * 检测 deepen-log 内容中是否包含饱和信号。
 * 用于 run 完成后决定是否提前停止循环。
 */
export function detectSaturation(logContent: string): boolean {
  return logContent.includes(SATURATION_SIGNAL);
}

/**
 * 将 "HH:MM" 格式的截止时间转为时间戳。
 * 如果今日该时间已过，设为明日同一时间。
 * 空输入返回 null。
 */
export function parseDeadlineInput(input: string): number | null {
  const trimmed = input.trim();
  if (!trimmed) return null;

  const match = trimmed.match(/^(\d{1,2}):(\d{2})$/);
  if (!match) return null;

  const hours = parseInt(match[1], 10);
  const minutes = parseInt(match[2], 10);
  if (hours > 23 || minutes > 59) return null;

  const now = new Date();
  const deadline = new Date(now);
  deadline.setHours(hours, minutes, 0, 0);

  // 如果今日该时间已过，设为明日
  if (deadline.getTime() <= now.getTime()) {
    deadline.setDate(deadline.getDate() + 1);
  }

  return deadline.getTime();
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `npx vitest run tests/unit/shared/deepen.test.ts`
Expected: 全部 PASS。

- [ ] **Step 5: typecheck**

Run: `npm run typecheck 2>&1 | grep -E "deepen|error TS" | grep -v mermaid`
Expected: 无输出（无错误）。

- [ ] **Step 6: Commit**

```bash
git add src/shared/deepen.ts tests/unit/shared/deepen.test.ts
git commit -m "feat: deepen 核心共享模块（维度/消息/饱和检测/截止时间解析）"
```

---

## Task 2: ChatPanel deepenMode 状态机

**Files:**
- Modify: `src/web/components/ChatPanel.tsx`

**背景：** ChatPanel 已有 `pendingRevise` 状态 + `REVISE_TO_CHAT_EVENT` 监听模式。deepenMode 遵循相同架构但状态更复杂——需要续轮逻辑和饱和检测。

- [ ] **Step 1: 读取 ChatPanel import 区和状态定义区**

Run: `read src/web/components/ChatPanel.tsx:1-30`（确认现有 import）
Run: `read src/web/components/ChatPanel.tsx:60-100`（确认现有 useState 定义）

确认现有 import 和 state 后继续。

- [ ] **Step 2: 加 import 和 deepenMode 状态**

在 ChatPanel.tsx 的 import 区（其他 `../../shared/` import 附近）加：

```typescript
import {
  DEEPEN_TO_CHAT_EVENT,
  type DeepenToChatDetail,
  buildDeepenMessage,
  detectSaturation,
  parseDeadlineInput,
} from '../../shared/deepen';
```

在现有 useState 定义区（`pendingRevise` 附近）加：

```typescript
  // 深化模式状态
  const [deepenMode, setDeepenMode] = useState<{
    active: boolean;
    stage: string;
    deadline: number;
    round: number;
    lastRunSucceeded: boolean;  // 跟踪上轮成败（连续失败检测）
    consecutiveFailures: number;
  } | null>(null);
  const [showDeepenDialog, setShowDeepenDialog] = useState(false);
  const [deepenStage, setDeepenStage] = useState<string>('');
  const [deadlineInput, setDeadlineInput] = useState('06:00');
  const prevIsRunningRef = useRef(false);
```

> 注：`useRef` 已在现有 import 中。确认 `import { ... useRef ... } from 'react'`。

- [ ] **Step 3: 加 DEEPEN_TO_CHAT_EVENT 监听**

在现有 `REVISE_TO_CHAT_EVENT` 的 useEffect 之后（约行 105 附近）加：

```typescript
  // 深化按钮：来自视图 🔁 按钮 dispatch 的事件
  useEffect(() => {
    const handler = (e: Event) => {
      const detail = (e as CustomEvent).detail as DeepenToChatDetail;
      setDeepenStage(detail.stage);
      setShowDeepenDialog(true);
    };
    window.addEventListener(DEEPEN_TO_CHAT_EVENT, handler);
    return () => window.removeEventListener(DEEPEN_TO_CHAT_EVENT, handler);
  }, []);
```

- [ ] **Step 4: 加 deepenMode 启动函数**

在 DEEPEN_TO_CHAT_EVENT 监听之后加：

```typescript
  /** 启动深化循环：从第 1 轮开始 */
  const startDeepen = useCallback(() => {
    const deadline = parseDeadlineInput(deadlineInput);
    if (!deadline) return;

    setDeepenMode({
      active: true,
      stage: deepenStage,
      deadline,
      round: 1,
      lastRunSucceeded: false,
      consecutiveFailures: 0,
    });
    setShowDeepenDialog(false);

    // 立即发第 1 轮
    const message = buildDeepenMessage(deepenStage, 1);
    sendMessage({
      projectId,
      agentId,
      skillId,
      stage: deepenStage,
      message,
      autonomous: true,
      model: selectedModel !== 'default' ? selectedModel : undefined,
    });
  }, [deadlineInput, deepenStage, sendMessage, projectId, agentId, skillId, selectedModel]);

  /** 退出深化模式 */
  const exitDeepen = useCallback((reason: string) => {
    if (!deepenMode) return;
    console.log(`[deepen] 循环结束：${reason}`);
    setDeepenMode(null);
  }, [deepenMode]);
```

- [ ] **Step 5: 加 run 完成后的续轮逻辑**

在现有 useEffect 区加（监听 isRunning 从 true→false 的转换）：

```typescript
  // 深化循环续轮：run 完成后（isRunning true→false）触发下一轮
  useEffect(() => {
    if (!deepenMode?.active) {
      prevIsRunningRef.current = isRunning;
      return;
    }

    // 检测 isRunning 从 true→false（run 刚完成）
    if (prevIsRunningRef.current && !isRunning) {
      // run 结束了。判断成败
      // 从 messages 最后一条判断是否成功（assistant 消息无 error 即成功）
      const lastMsg = chatMessages[chatMessages.length - 1];
      const succeeded = !lastMsg?.error;

      let consecutiveFailures = deepenMode.consecutiveFailures;
      if (!succeeded) {
        consecutiveFailures++;
      } else {
        consecutiveFailures = 0;
      }

      // 停止条件检查
      // 1. 连续 2 轮失败
      if (consecutiveFailures >= 2) {
        exitDeepen('连续 2 轮失败，疑似额度耗尽');
        prevIsRunningRef.current = isRunning;
        return;
      }

      // 2. 饱和检测：检查 deepen-log 是否有饱和信号
      //    通过 fetch 读项目文件
      (async () => {
        try {
          const res = await fetch(`/api/projects/${projectId}/files?path=${encodeURIComponent('deepen-log.md')}`);
          if (res.ok) {
            const data = await res.json();
            const logContent = data.content || '';
            if (detectSaturation(logContent)) {
              exitDeepen('饱和检测：各维度已达 4+ 分');
              prevIsRunningRef.current = isRunning;
              return;
            }
          }
        } catch { /* 读文件失败不阻断 */ }

        // 3. 时间检查
        if (Date.now() >= deepenMode.deadline) {
          exitDeepen('截止时间到');
          prevIsRunningRef.current = isRunning;
          return;
        }

        // 继续下一轮
        const nextRound = deepenMode.round + 1;
        setDeepenMode({ ...deepenMode, round: nextRound, consecutiveFailures });
        const message = buildDeepenMessage(deepenMode.stage, nextRound);
        sendMessage({
          projectId,
          agentId,
          skillId,
          stage: deepenMode.stage,
          message,
          autonomous: true,
          model: selectedModel !== 'default' ? selectedModel : undefined,
        });
      })();
    }

    prevIsRunningRef.current = isRunning;
  }, [isRunning, deepenMode, chatMessages, exitDeepen, projectId, agentId, skillId, selectedModel, sendMessage]);
```

- [ ] **Step 6: 用户发消息时退出 deepenMode**

在现有 `handleSend`（或发送消息的函数）开头加 deepenMode 检查。找到 ChatPanel 中处理用户发送的函数（通常调用 `sendMessage`），在函数体开头加：

```typescript
    // 用户手动发消息 → 退出深化模式
    if (deepenMode?.active) {
      exitDeepen('用户手动中断');
    }
```

> 注：需要先 `read` ChatPanel 确认 handleSend 的位置和名称（可能是内联或具名函数）。

- [ ] **Step 7: 加截止时间弹框 UI**

在 `{pendingRevise && ...}` 的 reviseBanner 之前加深化弹框：

```tsx
        {showDeepenDialog && (
          <div className={deepenDialog}>
            <div className={deepenDialogBody}>
              <span>🔁 深化「{deepenStage}」阶段</span>
              <label>
                截止时间：
                <input
                  type="text"
                  value={deadlineInput}
                  onChange={(e) => setDeadlineInput(e.target.value)}
                  placeholder="HH:MM（如 06:00）"
                  className={deepenInput}
                />
              </label>
              <div className={deepenDialogActions}>
                <button onClick={startDeepen} className={deepenConfirmBtn}>开始</button>
                <button onClick={() => setShowDeepenDialog(false)} className={deepenCancelBtn}>取消</button>
              </div>
            </div>
          </div>
        )}
```

- [ ] **Step 8: 加深化状态指示条**

在 `{pendingRevise && ...}` reviseBanner 之后加：

```tsx
        {deepenMode?.active && (
          <div className={deepenBanner}>
            <span>
              🔁 深化中 · 第 {deepenMode.round} 轮 · 截止 {new Date(deepenMode.deadline).toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' })}
            </span>
            <button className={reviseBannerClose} onClick={() => exitDeepen('手动停止')} title="停止深化循环">✕</button>
          </div>
        )}
```

- [ ] **Step 9: 加 CSS 样式**

在 ChatPanel.tsx 的 CSS 定义区（其他 `const xxx = css\`...\`` 附近）加：

```typescript
const deepenDialog = css`
  position: fixed; inset: 0;
  background: rgba(0,0,0,0.3);
  display: flex; align-items: center; justify-content: center;
  z-index: 1000;
`;

const deepenDialogBody = css`
  background: var(--haze-color-bg);
  border: 1px solid var(--haze-color-border);
  border-radius: 12px;
  padding: 1.5rem;
  display: flex; flex-direction: column;
  gap: 1rem;
  min-width: 320px;
`;

const deepenInput = css`
  margin-left: 0.5rem;
  padding: 0.25rem 0.5rem;
  border: 1px solid var(--haze-color-border);
  border-radius: 4px;
  background: var(--haze-color-bg);
  color: var(--haze-color-text);
  width: 80px;
`;

const deepenDialogActions = css`
  display: flex; gap: 0.5rem; justify-content: flex-end;
`;

const deepenConfirmBtn = css`
  padding: 0.4rem 1rem;
  border: none; border-radius: 6px;
  background: var(--haze-color-primary);
  color: white;
  cursor: pointer;
  font-size: 0.85rem;
  &:hover { opacity: 0.9; }
`;

const deepenCancelBtn = css`
  padding: 0.4rem 1rem;
  border: 1px solid var(--haze-color-border);
  border-radius: 6px;
  background: none;
  color: var(--haze-color-text);
  cursor: pointer;
  font-size: 0.85rem;
  &:hover { background: var(--haze-color-bg-secondary); }
`;

const deepenBanner = css`
  padding: 0.5rem 0.75rem;
  background: var(--haze-color-primary, #6366f1);
  opacity: 0.15;
  color: var(--haze-color-text);
  font-size: 0.8rem;
  display: flex; align-items: center; justify-content: space-between;
  border-radius: 4px 4px 0 0;
`;
```

> 注：`deepenBanner` 使用 primary 色 + 低透明度区分于 reviseBanner。`reviseBannerClose` 已存在，复用。

- [ ] **Step 10: typecheck**

Run: `npm run typecheck 2>&1 | grep -E "ChatPanel|error TS" | grep -v mermaid`
Expected: 无输出。

- [ ] **Step 11: 验证现有测试不受影响**

Run: `npx vitest run`
Expected: 全部 PASS（现有测试不受 ChatPanel 改动影响，因为 ChatPanel 无组件测试）。

- [ ] **Step 12: Commit**

```bash
git add src/web/components/ChatPanel.tsx
git commit -m "feat: ChatPanel deepenMode 状态机（事件监听+弹框+续轮+饱和检测+指示条）"
```

---

## Task 3: 视图按钮——CharacterView + WorldView + ConceptView

**背景：** 这三个视图已有 `useFileRevision`（含 stage），按钮模式一致。

**Files:**
- Modify: `src/web/components/views/CharacterView.tsx`
- Modify: `src/web/components/views/WorldView.tsx`
- Modify: `src/web/components/views/ConceptView.tsx`

- [ ] **Step 1: CharacterView 加深化按钮**

在 `src/web/components/views/CharacterView.tsx` 的 import 区加：

```typescript
import { DEEPEN_TO_CHAT_EVENT } from '../../shared/deepen';
```

在 header 按钮区（`<button className={reviseBtn}...>` 之前或之后）加：

```tsx
        <button
          className={reviseBtn}
          onClick={() => window.dispatchEvent(new CustomEvent(DEEPEN_TO_CHAT_EVENT, { detail: { stage: 'characters' } }))}
          title="自主循环深化角色阶段"
        >🔁 深化</button>
```

> 复用 `reviseBtn` 样式（与其他按钮一致）。

- [ ] **Step 2: WorldView 加深化按钮**

在 `src/web/components/views/WorldView.tsx` 的 import 区加：

```typescript
import { DEEPEN_TO_CHAT_EVENT } from '../../shared/deepen';
```

在 header 按钮区（`<button className={reviseBtn}...>` 附近）加：

```tsx
        <button
          className={reviseBtn}
          onClick={() => window.dispatchEvent(new CustomEvent(DEEPEN_TO_CHAT_EVENT, { detail: { stage: 'world' } }))}
          title="自主循环深化世界观阶段"
        >🔁 深化</button>
```

- [ ] **Step 3: ConceptView 加深化按钮**

在 `src/web/components/views/ConceptView.tsx` 的 import 区加：

```typescript
import { DEEPEN_TO_CHAT_EVENT } from '../../shared/deepen';
```

在 header 按钮区加：

```tsx
        <button
          className={reviseBtn}
          onClick={() => window.dispatchEvent(new CustomEvent(DEEPEN_TO_CHAT_EVENT, { detail: { stage: 'concept' } }))}
          title="自主循环深化概念阶段"
        >🔁 深化</button>
```

- [ ] **Step 4: typecheck**

Run: `npm run typecheck 2>&1 | grep -E "CharacterView|WorldView|ConceptView|error TS" | grep -v mermaid`
Expected: 无输出。

- [ ] **Step 5: Commit**

```bash
git add src/web/components/views/CharacterView.tsx src/web/components/views/WorldView.tsx src/web/components/views/ConceptView.tsx
git commit -m "feat: CharacterView/WorldView/ConceptView 加 🔁 深化按钮"
```

---

## Task 4: 视图按钮——OutlineView + SceneView

**背景：** 这两个视图没有 `useFileRevision`，但有 header 区。直接 dispatch 事件即可。

**Files:**
- Modify: `src/web/components/views/OutlineView.tsx`
- Modify: `src/web/components/views/SceneView.tsx`

- [ ] **Step 1: 读取 OutlineView header 区确认按钮模式**

Run: `read src/web/components/views/OutlineView.tsx:114-177`

确认 header 区结构后继续。

- [ ] **Step 2: OutlineView 加深化按钮**

在 `src/web/components/views/OutlineView.tsx` 的 import 区加：

```typescript
import { DEEPEN_TO_CHAT_EVENT } from '../../shared/deepen';
```

在 header 按钮区（如果有 `viewHeaderRow` 包裹的标题区）加按钮。如果 OutlineView 没有 `reviseBtn` 样式，用内联 style 或从 viewShared 引入：

```tsx
        <button
          onClick={() => window.dispatchEvent(new CustomEvent(DEEPEN_TO_CHAT_EVENT, { detail: { stage: 'outline' } }))}
          title="自主循环深化大纲阶段"
        >🔁 深化</button>
```

- [ ] **Step 3: 读取 SceneView header 区确认按钮模式**

Run: `read src/web/components/views/SceneView.tsx`（确认 header 区结构）

- [ ] **Step 4: SceneView 加深化按钮**

在 `src/web/components/views/SceneView.tsx` 的 import 区加：

```typescript
import { DEEPEN_TO_CHAT_EVENT } from '../../shared/deepen';
```

在 header 按钮区加：

```tsx
        <button
          onClick={() => window.dispatchEvent(new CustomEvent(DEEPEN_TO_CHAT_EVENT, { detail: { stage: 'scenes' } }))}
          title="自主循环深化场景阶段"
        >🔁 深化</button>
```

- [ ] **Step 5: typecheck**

Run: `npm run typecheck 2>&1 | grep -E "OutlineView|SceneView|error TS" | grep -v mermaid`
Expected: 无输出。

- [ ] **Step 6: Commit**

```bash
git add src/web/components/views/OutlineView.tsx src/web/components/views/SceneView.tsx
git commit -m "feat: OutlineView/SceneView 加 🔁 深化按钮"
```

---

## Task 5: 全量验证 + 手动冒烟测试

- [ ] **Step 1: 全量 typecheck**

Run: `npm run typecheck 2>&1 | grep "error TS" | grep -v mermaid`
Expected: 无输出（排除已知 mermaid 预存错误）。

- [ ] **Step 2: 全量测试**

Run: `npx vitest run`
Expected: 全部 PASS（含新增 deepen 测试）。

- [ ] **Step 3: 构建验证**

Run: `npm run build 2>&1 | tail -5`
Expected: 无错误（确认前端能构建）。

- [ ] **Step 4: 手动冒烟测试（需要 dev server）**

```bash
# 启动 dev server
pnpm dev &
# 等待启动
sleep 3
# 在浏览器打开（或用 curl 验证 API 可达）
curl -s http://localhost:3006/api/projects | head -c 100
```

在浏览器中：
1. 打开一个有 concept.md 的项目
2. 导航到角色页（CharacterView）
3. 确认看到 🔁 深化按钮
4. 点击 → 确认弹出截止时间弹框
5. 输入 06:00 → 点开始
6. 确认状态指示条显示"🔁 深化中 · 第 1 轮"
7. 等待 run 完成 → 确认自动进入第 2 轮
8. 在输入框发消息 → 确认深化循环中断

- [ ] **Step 5: Commit（如有修复）**

```bash
git add -A
git commit -m "fix: 冒烟测试发现的问题修复"
```

---

## 验收清单

- [ ] `src/shared/deepen.ts` 导出 DEEPEN_DIMENSIONS / buildDeepenMessage / detectSaturation / parseDeadlineInput / DEEPEN_TO_CHAT_EVENT / SATURATION_SIGNAL
- [ ] 5 个视图（ConceptView/WorldView/CharacterView/OutlineView/SceneView）各有 🔁 深化按钮
- [ ] 点击按钮弹出截止时间弹框（默认 06:00）
- [ ] 确认后进入 autonomous run，message 含结构化维度评分+验证回溯引导
- [ ] run 完成后自动续轮（isRunning true→false 触发）
- [ ] 每轮 message 带递增轮数
- [ ] 状态指示条显示当前轮数和截止时间
- [ ] 用户发消息时中断循环
- [ ] 点 ✕ 按钮中断循环
- [ ] 截止时间到时中断循环
- [ ] 连续 2 轮失败时中断循环
- [ ] deepen-log 含饱和信号时中断循环
- [ ] deepen-log 每轮含维度评分快照
- [ ] typecheck 通过（排除已知 mermaid 错误）
- [ ] 全量测试通过
- [ ] 构建通过
